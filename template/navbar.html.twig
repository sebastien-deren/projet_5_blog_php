<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-3 ">
	<div class="container-lg">
		<a href="/" class="navbar-brand " >
			{% if user %}
				{% if user.firstname and user.lastname %}
					{{user.firstname}} {{user.lastname}}
				{% else %}
					{{user.login}}
				{% endif %}
			{% else %}
				<img class="img-fluid " syle="color:black; width:100px;" src="/Assets/logoipsum.svg">
			{% endif %}
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="container-lg ">
			<div class="collapse navbar-collapse justify-content-end " id="navbarSupportedContent">
				<div class="text-align-center navbar-nav  mb-2 mb-lg-0 ">
					<a href="/" class="nav-link">Accueil</a>
					<a href="/blog" class="nav-link">Blog</a>
					{% if user.role.value == "admin" %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
								Admin
							</a>
							<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
								<li>
									<a class="dropdown-item" href="/admin/createpost">createpost</a>
									<a href="/admin/comment" class="dropdown-item">Comment moderation</a>
								</li>
							</ul>
						</li>
					{% endif %}
					{% if user %}
						<a href="/deconnection" class="nav-link">Déconnexion</a>
					{% else %}
						<a href="/connection" class="nav-link">Connexion</a>
					{% endif %}
				</div>
			</div>
		</div>
	</nav>
